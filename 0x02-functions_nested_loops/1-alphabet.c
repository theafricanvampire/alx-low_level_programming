#include <stdio.h>
/**
 * main -alphabet in lower case
 *
 * Return: always (0)
 */
int main(void)
{
	void print_alphabet(void);
	int x;
	{
	for (x='a'; x<='z'z; x++)
	_putchar(x);
	}
	_putchar('\n');
	return (0);
}
