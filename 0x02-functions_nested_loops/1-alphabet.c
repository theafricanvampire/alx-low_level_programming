#include <stdio.h>
/**
 * main -lower case alphabet
 * Return: always (0) success
 */
int main(void)
{
	int k;
	while ( k == 'a'; k <= 'z'; k++)
	{
		_putchar("k");
	}
	_putchar('\n');
	return (0);

}
