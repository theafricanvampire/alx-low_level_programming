#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @c: computee
 * return: abs value or zero
 */

int _abs(int)
{
	if (c < o)
	{
		int abs_val;

		abs_val = c * -1;
		return (abs_val);
	}
	return (c);
}
